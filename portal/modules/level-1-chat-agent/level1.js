(()=>{
"use strict";
const KEY="ai-project-education.level1.chat-agent.v2";
const initial=()=>({topicFit:"",expectedQuestions:"",responseScope:"",sourceRecords:"",name:"",user:"",role:"",sources:"",topics:"",boundaries:"",testQuestion:"",testResponse:"",testFinding:"",checks:{chatSuitable:false,boundedResponse:false,reliableSources:false,sourceUsable:false,stayedInRole:false,usedSources:false,askedAgain:false}});
let state=initial();
const $=s=>document.querySelector(s);
const clean=v=>String(v??"").trim();
function load(){try{const saved=JSON.parse(localStorage.getItem(KEY));if(saved)state={...initial(),...saved,checks:{...initial().checks,...saved.checks}}}catch(_){}}
function save(){try{localStorage.setItem(KEY,JSON.stringify(state));$("#saveStatus").textContent="이 브라우저에 자동 저장됨"}catch(_){$("#saveStatus").textContent="브라우저 저장을 사용할 수 없음"}}
function feasibilityMissing(){const missing=[];if(!clean(state.topicFit))missing.push("대화 주제와 사용 상황");if(!clean(state.expectedQuestions))missing.push("예상 질문");if(!clean(state.responseScope))missing.push("답변 범위");if(!clean(state.sourceRecords))missing.push("자료 목록");[["chatSuitable","대화 주제 적합성"],["boundedResponse","답변 범위 구분"],["reliableSources","신뢰 자료 2건"],["sourceUsable","자료 사용 조건"]].forEach(([key,label])=>{if(!state.checks[key])missing.push(label)});return missing}
function prompt(){return[`너는 '${clean(state.name)||"대화봇"}'이다.`,`사용자: ${clean(state.user)||"아직 정하지 않음"}`,`대화 주제와 사용 상황: ${clean(state.topicFit)}`,`예상 질문: ${clean(state.expectedQuestions)}`,`역할: ${clean(state.role)||"아직 정하지 않음"}`,`답변 근거·자료 범위: ${clean(state.sources)||"아직 정하지 않음"}`,`확보한 자료 목록: ${clean(state.sourceRecords)}`,`답변할 수 있는 범위: ${clean(state.responseScope)}`,`답변 주제: ${clean(state.topics)||"아직 정하지 않음"}`,`금지·재질문 규칙: ${clean(state.boundaries)||"아직 정하지 않음"}`,"","제공된 자료와 범위를 벗어난 사실·인용·출처를 만들지 마라. 근거가 부족하면 모른다고 말하고, 필요한 정보를 짧게 재질문하라.","자료를 실제로 검색하거나 찾았다고 말하지 마라. 답변은 대화 상대의 말투를 쓰되, 사용자가 스스로 생각할 수 있는 질문을 하나 포함하라."].join("\n")}
function refresh(){const missing=feasibilityMissing();$("#agentPreview").textContent=clean(state.name)||"나의 대화봇";$("#rolePreview").textContent=clean(state.role)||"아직 정하지 않음";$("#sourcePreview").textContent=clean(state.sources)||"아직 정하지 않음";$("#boundaryPreview").textContent=clean(state.boundaries)||"아직 정하지 않음";$("#prompt").textContent=prompt();$("#copyPrompt").disabled=missing.length>0;$("#feasibilityState").textContent=missing.length?`프롬프트 작성 전 확인할 항목: ${missing.join(" / ")}`:"시작 판단 통과: 대화 규칙을 설계하고 외부 AI에서 첫 대화를 검증하세요."}
function bind(){Object.keys(state).filter(k=>k!=="checks").forEach(key=>{const input=$(`[data-bind="${key}"]`);if(input){input.value=state[key];input.addEventListener("input",()=>{state[key]=input.value;save();refresh()})}});Object.keys(state.checks).forEach(key=>{const input=$(`[data-check="${key}"]`);if(input){input.checked=state.checks[key];input.addEventListener("change",()=>{state.checks[key]=input.checked;save();refresh()})}})}
function packageMarkdown(){return`# ${clean(state.name)||"LEVEL 1 대화형 미니 에이전트"}\n\n## 시작 판단\n- 대화 주제와 사용 상황: ${clean(state.topicFit)}\n- 예상 질문: ${clean(state.expectedQuestions)}\n- 답변 범위: ${clean(state.responseScope)}\n- 자료 목록: ${clean(state.sourceRecords)}\n- 대화 주제 적합성: ${state.checks.chatSuitable}\n- 답변 범위 구분: ${state.checks.boundedResponse}\n- 신뢰 자료 2건: ${state.checks.reliableSources}\n- 자료 사용 조건: ${state.checks.sourceUsable}\n\n## 프로젝트\n- 사용자: ${clean(state.user)}\n- 역할: ${clean(state.role)}\n- 답변 근거·자료 범위: ${clean(state.sources)}\n- 답변 주제: ${clean(state.topics)}\n- 금지·재질문 규칙: ${clean(state.boundaries)}\n\n## 대화 프롬프트\n\`\`\`\n${prompt()}\n\`\`\`\n\n## 테스트 기록\n- 질문: ${clean(state.testQuestion)}\n- 실제 응답 요약: ${clean(state.testResponse)}\n- 발견한 문제·수정할 규칙: ${clean(state.testFinding)}\n- 역할·주제 준수: ${state.checks.stayedInRole}\n- 출처 꾸며내지 않음: ${state.checks.usedSources}\n- 재질문·답변 범위 표시: ${state.checks.askedAgain}\n\n## 알려진 제한\n- 이 패키지는 외부 채팅 AI에 수동으로 붙여 넣어 사용한다.\n- 실제 문서 검색, 출처 자동 연결, 파일 업로드, API 호출은 포함하지 않는다.\n- AI 응답은 사람이 근거와 규칙 준수 여부를 검토한다.\n`}
function download(){const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([packageMarkdown()],{type:"text/markdown;charset=utf-8"}));a.download="level-1-chat-agent-package.md";document.body.append(a);a.click();const url=a.href;a.remove();setTimeout(()=>URL.revokeObjectURL(url),0)}
$("#copyPrompt").onclick=async()=>{try{await navigator.clipboard.writeText(prompt());$("#copyPrompt").textContent="복사됨";setTimeout(()=>$("#copyPrompt").textContent="프롬프트 복사",1200)}catch(_){alert("복사할 수 없습니다. 프롬프트 영역을 직접 선택해 복사하세요.")}};
$("#downloadPackage").onclick=download;
load();bind();refresh();
})();
